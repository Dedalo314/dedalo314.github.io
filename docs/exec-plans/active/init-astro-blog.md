# Execution Plan: Initialize Astro Blog for Math, Physics, and AI

## Objectives

- Scaffold an Astro project suitable for a personal blog.
- Enable LaTeX support for mathematical formulas.
- Enable dynamic component support for animations (React/Vue/Svelte or vanilla JS).
- Update project documentation (`AGENTS.md`, `docs/`) to reflect the new architecture.

## Tasks

1. [x] Scaffold Astro project manually (due to internet issues with create-astro).
2. [x] Install and configure LaTeX support (remark-math, rehype-katex).
3. [x] Set up a basic blog structure (content collections for posts).
4. [x] Implement a sample post with LaTeX and a dynamic animation (React particle system).
5. [x] Update `AGENTS.md` to reflect the Astro stack.
6. [x] Update `docs/design-docs/index.md` to reflect Astro's architecture.
7. [x] Clean up obsolete documentation inherited from the previous project.

## Verification Steps

- Run `npm run dev` and verify the site loads.
- Verify LaTeX renders correctly in a sample post.
- Verify a dynamic animation component (e.g., a simple JS-driven canvas or React component) runs.
- Check that all links in `AGENTS.md` point to existing, relevant documentation.
